#version 400 core

in vec3 position;
in vec2 textureCoords;

out vec3 colour;

uniform mat4 projectionViewMatrix;

void main(void){

	gl_Position = projectionViewMatrix * vec4(position.x, position.y, position.z, 1.0);
	colour = vec3(position.y*0.06, 0.8-0.07*position.y, position.y*0.015);

}